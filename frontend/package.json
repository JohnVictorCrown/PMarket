{
  "name": "quant-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun build src/main.js --outdir=dist --target=browser --watch & tailwindcss -i src/app.css -o dist/app.css --watch",
    "build": "bun run build.js",
    "preview": "npx serve dist"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "svelte": "^5.0.0"
  },
  "dependencies": {
    "@skeletonlabs/skeleton": "^3.0.0",
    "@skeletonlabs/tw-plugin": "^0.4.0"
  }
}
